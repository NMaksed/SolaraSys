import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sem: {
    height: '100vh',
    width: '100%',
    // Outros estilos que você deseja aplicar ao NavigationContainer
  },
  // Outros estilos podem ser definidos aqui
});

export default styles;