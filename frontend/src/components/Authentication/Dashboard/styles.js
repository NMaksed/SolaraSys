import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tabela: {
        paddingTop: 100,
    },
    container: {
        width: 1000,
        height: 300,

    }
    });

export default styles;
